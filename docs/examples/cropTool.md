## Crop tool
There is an option to crop the image before you upload it. You only need to add `cropTool` attribute
```jsx render
  <div>
    <FileInput
      label="File Input"
      cropTool
    />
  </div>
```

